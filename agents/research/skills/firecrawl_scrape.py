"""Firecrawl `scrape_url` skill.

Full call signature in `docs/INTEGRATIONS.md#firecrawl`.
Cache layer uses `public.firecrawl_cache` keyed by `sha256(url)` with 7-day TTL.
Implementation: Phase 2.
"""
